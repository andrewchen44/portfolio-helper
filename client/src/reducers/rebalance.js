const initialState = [
  [0, 0, 0, 0, 0],
];

const calculate = (actual, ideal) {
  // return the calulcated rebalance otal
}

export default function rebalance(state = initialState, action) {
  switch(action.type) {
    default:
      return state;
  }
}